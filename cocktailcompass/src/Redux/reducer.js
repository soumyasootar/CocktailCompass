const initalstate= {
    cocktails:[],
    loading:false,
    error:null
}

const cockTailReducer =(state=initalstate,action)=>{

    switch (action.type){

        default:
            return state
    }

}
export default cockTailReducer
